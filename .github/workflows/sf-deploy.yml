name: SF Deploy

on:
  push:
    branches:
      - "promote/**"
      - "rollback/**"
      - "feature/*-to-*-resolve-*"
  pull_request:
    types: [opened, synchronize, reopened]

jobs:

  # ── qa → production
  validate_qa_to_production:
    runs-on: ubuntu-latest
    if: >-
      startsWith(github.ref_name, 'promote/qa-to-production-')
      || startsWith(github.ref_name, 'feature/qa-to-production-resolve-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_PRODUCTION }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_PRODUCTION }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_PRODUCTION }} --instance-url ${{ vars.SF_ORG_URL_PRODUCTION }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun
      - name: Notify HAV-IQ
        if: failure()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key
  deploy_qa_to_production:
    needs: validate_qa_to_production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_PRODUCTION }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_PRODUCTION }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_PRODUCTION }} --instance-url ${{ vars.SF_ORG_URL_PRODUCTION }} --alias target
          if git log -1 --pretty=%B | grep -q "\[run-tests\]"; then
            for i in 1 2 3; do sf apex run test --target-org target --test-level RunLocalTests --wait 10 && break; [ $i -lt 3 ] && echo "Test run busy, retrying in 60s..." && sleep 60 || exit 1; done
          fi
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          sf project deploy start --manifest manifest/package.xml --target-org target
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── production → qa (back-promote)
  back_promote_production_to_qa:
    runs-on: ubuntu-latest
    if: startsWith(github.ref_name, 'promote/production-to-qa-')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 20
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - name: Authenticate to the back-promote target org
        run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - name: Back-promote to qa
        run: |
          sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun --wait 30
      - name: Apply destructive changes
        run: |
          if [ -s manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml; then
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --post-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          else
            echo "No destructive changes."
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key
  deploy_destructive_qa_to_production:
    needs: deploy_qa_to_production
    runs-on: ubuntu-latest
    if: >-
      startsWith(github.ref_name, 'promote/qa-to-production-')
      || startsWith(github.ref_name, 'feature/qa-to-production-resolve-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_PRODUCTION }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_PRODUCTION }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_PRODUCTION }} --instance-url ${{ vars.SF_ORG_URL_PRODUCTION }} --alias target
          if [ ! -f manifest/destructiveChanges.xml ] || ! grep -q "<members>" manifest/destructiveChanges.xml; then
            echo "No destructive changes for this deploy — nothing to do."; exit 0
          fi
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          if [ ! -f manifest/destructiveChanges.xml ] || ! grep -q "<members>" manifest/destructiveChanges.xml; then
            echo "No destructive changes for this deploy — nothing to do."; exit 0
          fi
          set +e
          DOUT=$(sf project deploy start --manifest manifest/package.xml --pre-destructive-changes manifest/destructiveChanges.xml --target-org target --wait 30 2>&1)
          DRC=$?
          set -e
          echo "$DOUT"
          if [ $DRC -ne 0 ]; then
            ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
            OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
            if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
              echo "[OI-317] Deletions skipped - already absent from the target org:"
              echo "$ABSENT"
              echo "[OI-317] The intended end state is already true. Continuing."
              echo "$ABSENT" > /tmp/haviq-already-absent.txt
            else
              echo "[OI-317] Destructive deploy failed beyond already-absent components."
              exit $DRC
            fi
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── dev1 → qa
  validate_dev1_to_qa:
    runs-on: ubuntu-latest
    if: >-
      startsWith(github.ref_name, 'promote/dev1-to-qa-')
      || startsWith(github.ref_name, 'feature/dev1-to-qa-resolve-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun
      - name: Notify HAV-IQ
        if: failure()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key
  deploy_dev1_to_qa:
    needs: validate_dev1_to_qa
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
          if git log -1 --pretty=%B | grep -q "\[run-tests\]"; then
            for i in 1 2 3; do sf apex run test --target-org target --test-level RunLocalTests --wait 10 && break; [ $i -lt 3 ] && echo "Test run busy, retrying in 60s..." && sleep 60 || exit 1; done
          fi
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          sf project deploy start --manifest manifest/package.xml --target-org target
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── qa → dev1 (back-promote)
  back_promote_qa_to_dev1:
    runs-on: ubuntu-latest
    if: startsWith(github.ref_name, 'promote/qa-to-dev1-')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 20
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - name: Authenticate to the back-promote target org
        run: |
          echo "${{ secrets.SF_JWT_KEY_DEV1 }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_DEV1 }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_DEV1 }} --instance-url ${{ vars.SF_ORG_URL_DEV1 }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - name: Back-promote to dev1
        run: |
          sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun --wait 30
      - name: Apply destructive changes
        run: |
          if [ -s manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml; then
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --post-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          else
            echo "No destructive changes."
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key
  deploy_destructive_dev1_to_qa:
    needs: deploy_dev1_to_qa
    runs-on: ubuntu-latest
    if: >-
      startsWith(github.ref_name, 'promote/dev1-to-qa-')
      || startsWith(github.ref_name, 'feature/dev1-to-qa-resolve-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
          if [ ! -f manifest/destructiveChanges.xml ] || ! grep -q "<members>" manifest/destructiveChanges.xml; then
            echo "No destructive changes for this deploy — nothing to do."; exit 0
          fi
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          if [ ! -f manifest/destructiveChanges.xml ] || ! grep -q "<members>" manifest/destructiveChanges.xml; then
            echo "No destructive changes for this deploy — nothing to do."; exit 0
          fi
          set +e
          DOUT=$(sf project deploy start --manifest manifest/package.xml --pre-destructive-changes manifest/destructiveChanges.xml --target-org target --wait 30 2>&1)
          DRC=$?
          set -e
          echo "$DOUT"
          if [ $DRC -ne 0 ]; then
            ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
            OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
            if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
              echo "[OI-317] Deletions skipped - already absent from the target org:"
              echo "$ABSENT"
              echo "[OI-317] The intended end state is already true. Continuing."
              echo "$ABSENT" > /tmp/haviq-already-absent.txt
            else
              echo "[OI-317] Destructive deploy failed beyond already-absent components."
              exit $DRC
            fi
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── dev → qa
  validate_dev_to_qa:
    runs-on: ubuntu-latest
    if: >-
      startsWith(github.ref_name, 'promote/dev-to-qa-')
      || startsWith(github.ref_name, 'feature/dev-to-qa-resolve-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun
      - name: Notify HAV-IQ
        if: failure()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key
  deploy_dev_to_qa:
    needs: validate_dev_to_qa
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
          if git log -1 --pretty=%B | grep -q "\[run-tests\]"; then
            for i in 1 2 3; do sf apex run test --target-org target --test-level RunLocalTests --wait 10 && break; [ $i -lt 3 ] && echo "Test run busy, retrying in 60s..." && sleep 60 || exit 1; done
          fi
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          sf project deploy start --manifest manifest/package.xml --target-org target
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── qa → dev (back-promote)
  back_promote_qa_to_dev:
    runs-on: ubuntu-latest
    if: startsWith(github.ref_name, 'promote/qa-to-dev-')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 20
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - name: Authenticate to the back-promote target org
        run: |
          echo "${{ secrets.SF_JWT_KEY_DEV }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_DEV }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_DEV }} --instance-url ${{ vars.SF_ORG_URL_DEV }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - name: Back-promote to dev
        run: |
          sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun --wait 30
      - name: Apply destructive changes
        run: |
          if [ -s manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml; then
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --post-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          else
            echo "No destructive changes."
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key
  deploy_destructive_dev_to_qa:
    needs: deploy_dev_to_qa
    runs-on: ubuntu-latest
    if: >-
      startsWith(github.ref_name, 'promote/dev-to-qa-')
      || startsWith(github.ref_name, 'feature/dev-to-qa-resolve-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
          if [ ! -f manifest/destructiveChanges.xml ] || ! grep -q "<members>" manifest/destructiveChanges.xml; then
            echo "No destructive changes for this deploy — nothing to do."; exit 0
          fi
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          if [ ! -f manifest/destructiveChanges.xml ] || ! grep -q "<members>" manifest/destructiveChanges.xml; then
            echo "No destructive changes for this deploy — nothing to do."; exit 0
          fi
          set +e
          DOUT=$(sf project deploy start --manifest manifest/package.xml --pre-destructive-changes manifest/destructiveChanges.xml --target-org target --wait 30 2>&1)
          DRC=$?
          set -e
          echo "$DOUT"
          if [ $DRC -ne 0 ]; then
            ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
            OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
            if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
              echo "[OI-317] Deletions skipped - already absent from the target org:"
              echo "$ABSENT"
              echo "[OI-317] The intended end state is already true. Continuing."
              echo "$ABSENT" > /tmp/haviq-already-absent.txt
            else
              echo "[OI-317] Destructive deploy failed beyond already-absent components."
              exit $DRC
            fi
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── Rollback: dev
  rollback_dev:
    runs-on: ubuntu-latest
    if: startsWith(github.ref_name, 'rollback/dev-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_DEV }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_DEV }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_DEV }} --instance-url ${{ vars.SF_ORG_URL_DEV }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          # OI-278 (v1.50): single sf project deploy start per rollback job
          HAS_DEL=$( [ -f manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml && echo 1 || echo 0 )
          HAS_PKG=$( [ -f manifest/package.xml ] && grep -q "<members>" manifest/package.xml && echo 1 || echo 0 )
          if [ "$HAS_DEL" = "1" ]; then
            echo "Rollback: reverting (destructive + restore in one operation)..."
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --pre-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          elif [ "$HAS_PKG" = "1" ]; then
            echo "Rollback: restoring components..."
            sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun --wait 30
          else
            echo "Rollback: nothing to deploy (empty manifests)."
            exit 1
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── Rollback: dev1
  rollback_dev1:
    runs-on: ubuntu-latest
    if: startsWith(github.ref_name, 'rollback/dev1-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_DEV1 }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_DEV1 }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_DEV1 }} --instance-url ${{ vars.SF_ORG_URL_DEV1 }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          # OI-278 (v1.50): single sf project deploy start per rollback job
          HAS_DEL=$( [ -f manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml && echo 1 || echo 0 )
          HAS_PKG=$( [ -f manifest/package.xml ] && grep -q "<members>" manifest/package.xml && echo 1 || echo 0 )
          if [ "$HAS_DEL" = "1" ]; then
            echo "Rollback: reverting (destructive + restore in one operation)..."
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --pre-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          elif [ "$HAS_PKG" = "1" ]; then
            echo "Rollback: restoring components..."
            sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun --wait 30
          else
            echo "Rollback: nothing to deploy (empty manifests)."
            exit 1
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── Rollback: qa
  rollback_qa:
    runs-on: ubuntu-latest
    if: startsWith(github.ref_name, 'rollback/qa-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_QA }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_QA }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_QA }} --instance-url ${{ vars.SF_ORG_URL_QA }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          # OI-278 (v1.50): single sf project deploy start per rollback job
          HAS_DEL=$( [ -f manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml && echo 1 || echo 0 )
          HAS_PKG=$( [ -f manifest/package.xml ] && grep -q "<members>" manifest/package.xml && echo 1 || echo 0 )
          if [ "$HAS_DEL" = "1" ]; then
            echo "Rollback: reverting (destructive + restore in one operation)..."
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --pre-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          elif [ "$HAS_PKG" = "1" ]; then
            echo "Rollback: restoring components..."
            sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun --wait 30
          else
            echo "Rollback: nothing to deploy (empty manifests)."
            exit 1
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── Rollback: production
  rollback_production:
    runs-on: ubuntu-latest
    if: startsWith(github.ref_name, 'rollback/production-')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - run: |
          echo "${{ secrets.SF_JWT_KEY_PRODUCTION }}" > server.key
          sf org login jwt --client-id ${{ vars.SF_CLIENT_ID_PRODUCTION }} --jwt-key-file server.key --username ${{ vars.SF_USERNAME_PRODUCTION }} --instance-url ${{ vars.SF_ORG_URL_PRODUCTION }} --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - run: |
          # OI-278 (v1.50): single sf project deploy start per rollback job
          HAS_DEL=$( [ -f manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml && echo 1 || echo 0 )
          HAS_PKG=$( [ -f manifest/package.xml ] && grep -q "<members>" manifest/package.xml && echo 1 || echo 0 )
          if [ "$HAS_DEL" = "1" ]; then
            echo "Rollback: reverting (destructive + restore in one operation)..."
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --pre-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          elif [ "$HAS_PKG" = "1" ]; then
            echo "Rollback: restoring components..."
            sf project deploy start --manifest manifest/package.xml --target-org target --test-level NoTestRun --wait 30
          else
            echo "Rollback: nothing to deploy (empty manifests)."
            exit 1
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key

  # ── Feature PR → target stage (RM-Z8)
  deploy_feature:
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'pull_request'
      && startsWith(github.head_ref, 'feature/')
      && !contains(github.head_ref, '-resolve-')
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 20
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g @salesforce/cli
      - name: Authenticate to the PR target org
        env:
          HAVIQ_SECRETS: ${{ toJSON(secrets) }}
          HAVIQ_VARS: ${{ toJSON(vars) }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          TGT=$(echo "$BASE_REF" | tr "[:lower:]" "[:upper:]" | sed "s/[^A-Z0-9]/_/g")
          K=$(echo "$HAVIQ_SECRETS" | jq -r --arg k "SF_JWT_KEY_$TGT"   '.[$k] // empty')
          C=$(echo "$HAVIQ_VARS"    | jq -r --arg k "SF_CLIENT_ID_$TGT" '.[$k] // empty')
          U=$(echo "$HAVIQ_VARS"    | jq -r --arg k "SF_USERNAME_$TGT"  '.[$k] // empty')
          O=$(echo "$HAVIQ_VARS"    | jq -r --arg k "SF_ORG_URL_$TGT"   '.[$k] // empty')
          if [ -z "$K" ]; then
            echo "Missing GitHub Actions SECRET SF_JWT_KEY_$TGT (target branch $BASE_REF)."
            echo "HAV-IQ cannot push secrets — add it under Settings > Secrets and variables > Actions > Secrets."
            exit 1
          fi
          if [ -z "$C" ] || [ -z "$U" ] || [ -z "$O" ]; then
            echo "No SF_*_$TGT variables for target branch $BASE_REF — is it a configured stage?"; exit 1;
          fi
          echo "$K" > server.key
          sf org login jwt --client-id "$C" --jwt-key-file server.key --username "$U" --instance-url "$O" --alias target
      - name: Resolve HAV-IQ manifest
        env:
          HAVIQ_VARS: ${{ toJSON(vars) }}
        run: |
          TOK=$(git log -20 --pretty=%B | grep -oE "\[haviq-dep:[A-Za-z0-9]+\]" | head -1 | sed -E "s/.*:([A-Za-z0-9]+)\]/\1/")
          if [ -z "$TOK" ]; then
            if [ -s manifest/package.xml ]; then
              echo "No [haviq-dep:] token; falling back to the manifest on the branch."
              exit 0
            fi
            echo "ERROR: no manifest on the branch and no [haviq-dep:] token in the commit message"; exit 1
          fi
          echo "SF_DEPLOYMENT_ID=$TOK" >> "$GITHUB_ENV"
          # GitHub stores Actions variable names UPPERCASED, and deploy ids are
          # mixed case — look up the uppercased name or the lookup silently misses.
          TOK=$(echo "$TOK" | tr "[:lower:]" "[:upper:]")
          mkdir -p manifest
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_PKG_$TOK" '.[$k] // empty' > manifest/package.xml
          if [ ! -s manifest/package.xml ]; then echo "ERROR: variable HAVIQ_PKG_$TOK not set"; exit 1; fi
          echo "$HAVIQ_VARS" | jq -r --arg k "HAVIQ_DESTRUCTIVE_$TOK" '.[$k] // empty' > manifest/destructiveChanges.xml
          echo "[OI-292] manifest (dep $TOK):"; cat manifest/package.xml
      - name: Deploy to the PR target org
        run: |
          MSG=$(git log -20 --pretty=%B)
          LEVEL=$(echo "$MSG" | grep -oE "\[test-level:[A-Za-z]+\]" | head -1 | sed -E "s/\[test-level:([A-Za-z]+)\]/\1/" || true)
          if [ -z "$LEVEL" ]; then echo "$MSG" | grep -q "\[run-tests\]" && LEVEL=RunLocalTests || LEVEL=NoTestRun; fi
          echo "$MSG" | grep -q "\[validate-only\]" && DCMD=validate || DCMD=start
          if [ "$LEVEL" = "RunSpecifiedTests" ]; then
            TESTS=$(echo "$MSG" | grep -oE "\[tests:[^]]+\]" | head -1 | sed -E "s/\[tests:(.*)\]/\1/" || true)
            TF=""; for t in $(echo "$TESTS" | tr "," " "); do TF="$TF --tests $t"; done
            sf project deploy $DCMD --manifest manifest/package.xml --target-org target --test-level RunSpecifiedTests $TF --wait 30
          else
            sf project deploy $DCMD --manifest manifest/package.xml --target-org target --test-level "$LEVEL" --wait 30
          fi
      - name: Apply destructive changes
        run: |
          if [ -s manifest/destructiveChanges.xml ] && grep -q "<members>" manifest/destructiveChanges.xml; then
            echo "Applying destructive changes..."
            set +e
            DOUT=$(sf project deploy start --manifest manifest/package.xml --post-destructive-changes manifest/destructiveChanges.xml --target-org target --test-level NoTestRun --wait 30 2>&1)
            DRC=$?
            set -e
            echo "$DOUT"
            if [ $DRC -ne 0 ]; then
              ABSENT=$(echo "$DOUT" | grep -oE "No [A-Za-z]+ named:? [^ ]+ found" | sort -u)
              OTHER=$(echo "$DOUT" | grep -iE "error|Problem" | grep -vcE "No [A-Za-z]+ named:? [^ ]+ found")
              if [ -n "$ABSENT" ] && [ "$OTHER" -eq 0 ]; then
                echo "[OI-317] Deletions skipped - already absent from the target org:"
                echo "$ABSENT"
                echo "[OI-317] The intended end state is already true. Continuing."
                echo "$ABSENT" > /tmp/haviq-already-absent.txt
              else
                echo "[OI-317] Destructive deploy failed beyond already-absent components."
                exit $DRC
              fi
            fi
          else
            echo "No destructive changes."
          fi
      - name: Notify HAV-IQ
        if: always()
        env:
          SF_JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SF_WEBHOOK_SECRET: ${{ secrets.SF_WEBHOOK_SECRET }}
          SF_BRANCH: ${{ github.head_ref || github.ref_name }}
          SF_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          STATUS=$([ "$SF_JOB_STATUS" = "success" ] && echo "passed" || echo "failed")
          ABSENT_JSON=""
          if [ -f /tmp/haviq-already-absent.txt ]; then
            ABSENT_JSON=$(python3 -c "import json;t=open('/tmp/haviq-already-absent.txt').read().strip();print(',\"alreadyAbsent\":'+json.dumps(t) if t else '')" 2>/dev/null || echo "")
          fi
          # OI-209: capture deploy failure reason from job log
          FAILURE_REASON_JSON=""
          if [ "$STATUS" = "failed" ]; then
            TRACE=$(curl -sf -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ github.job }}/logs" 2>/dev/null || echo "")
            MATCHED=$(echo "$TRACE" | grep -E "Problem|No such column|Variable does not exist|Component Failures|FAILED|✘" | head -40)
            # Fallback: a fixed keyword list always misses the next novel error — keep the trace tail so the reason is never blank.
            if [ -z "$MATCHED" ]; then MATCHED=$(echo "$TRACE" | grep -v "^[[:space:]]*$" | tail -40); fi
            FAILURE_REASON_JSON=$(echo "$MATCHED" | sed "s/\x1b\[[0-9;]*m//g" | head -c 30000 | python3 -c "import sys,json;t=sys.stdin.read().strip();print(json.dumps(t) if t else \"\")" 2>/dev/null || echo "")
          fi
          echo "${{ secrets.HAVIQ_PRIVATE_KEY }}" > /tmp/haviq.key
          sf org login jwt --client-id "${{ vars.HAVIQ_CLIENT_ID }}" --jwt-key-file /tmp/haviq.key --username "${{ vars.HAVIQ_USERNAME }}" --instance-url "${{ vars.HAVIQ_URL }}" --alias haviq-hub
          SF_TOKEN=$(sf org auth show-access-token --target-org haviq-hub --json | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['result']['accessToken'])" 2>/dev/null || echo "")
          if [ -n "$SF_TOKEN" ]; then
            if [ -n "$FAILURE_REASON_JSON" ]; then
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\",\"failureReason\":$FAILURE_REASON_JSON$ABSENT_JSON}"
            else
              BODY="{\"branch\":\"$SF_BRANCH\",\"commitSha\":\"$SF_COMMIT_SHA\",\"deploymentId\":\"$SF_DEPLOYMENT_ID\",\"status\":\"$STATUS\"$ABSENT_JSON}"
            fi
            HAVIQ_HTTP=$(curl -s -o /tmp/haviq_cb.out -w "%{http_code}" -X POST "${{ vars.HAVIQ_URL }}/services/apexrest/haviq/pipeline/webhook" -H "Authorization: Bearer $SF_TOKEN" -H "Content-Type: application/json" -H "x-sf-devops-secret: $SF_WEBHOOK_SECRET" -d "$BODY" || echo "000")
            echo "HAV-IQ callback HTTP $HAVIQ_HTTP"; cat /tmp/haviq_cb.out 2>/dev/null || true
            if [ "$HAVIQ_HTTP" = "401" ]; then echo "Callback rejected: check the SF_WEBHOOK_SECRET repository secret matches the pipeline webhook secret."; fi
          fi
          rm -f /tmp/haviq.key
